<UserControl x:Class="MarketData.Wpf.Client.Views.ModelConfigs.RandomAdditiveWalkConfigView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewmodels="clr-namespace:MarketData.Wpf.Client.ViewModels.ModelConfigs"
             d:DataContext="{d:DesignInstance Type=viewmodels:RandomAdditiveWalkConfigViewModel}"
             mc:Ignorable="d">
    <StackPanel>
        <TextBlock Text="Random Additive Walk Configuration" 
                   FontWeight="SemiBold"
                   FontSize="14"
                   Foreground="Orange"
                   Margin="10,15,10,5"/>
        
        <TextBlock Text="{Binding StepCount, StringFormat='Walk Steps: {0}'}" 
                   Foreground="White"
                   Margin="20,5,10,10"/>
        
        <ItemsControl ItemsSource="{Binding WalkSteps}" Margin="20,0">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="0,5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="100"/>
                        </Grid.ColumnDefinitions>
                        
                        <StackPanel Grid.Column="0" Orientation="Horizontal">
                            <TextBlock Text="Probability: " Foreground="LightGray"/>
                            <TextBlock Text="{Binding ProbabilityPercentage}" Foreground="White"/>
                        </StackPanel>
                        
                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                            <TextBlock Text="Step: " Foreground="LightGray"/>
                            <TextBlock Text="{Binding StepValue, StringFormat='{}{0:F4}'}" Foreground="White"/>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </StackPanel>
</UserControl>
